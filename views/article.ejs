<%- include ('partials/header.ejs') %>

<body>
    <%- include ('partials/homenavbar.ejs') %>
    <br>
    <div class="container">
        <hr class="bg-secondary">
        <% articles.forEach((article) => { %>
            <div class="card">
                <div class="card-header">
                    <h3> <%= article.title %> </h3>
                </div>
                <div class="card-body">
                    <a class="btn btn-info" href="/articles/<%= article.slug %>">Ler Artigo</a>
                </div>
            </div>
            <br>
        <% }); %>
        <div class="row">
            <div class="col">
                <% if(page > 2) { %>
                    <a class="btn btn-info" href="/articles/page/<%= page - 1 %>"><< Página Anterior</a>
                    
                    <% } else { %>
                        <a class="btn btn-info" href="/"><< Página Anterior</a>
                    <% } %>
                <% if(next) { %>
                    <a class="btn btn-info" href="/articles/page/<%= page + 1 %>" style="float:right">Próxima Página >></a>
                <% } %>
            </div>
            
        </div>
        <hr class="bg-secondary">
    </div>
</body>

<%- include ('partials/footer.ejs') %>